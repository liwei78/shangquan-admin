= form_for(user) do |f|
  %table.styled
    %tbody
      %tr
        %td
          登录账号
        %td
          = f.text_field :email, :class => "input"
      %tr
        %td
          登录密码
        %td
          = f.password_field :password, :class => "input"
      %tr
        %td
          显示名称
        %td
          = f.text_field :name, :class => "input"
      %tr
        %td
          发布权限
        %td
          = f.select :promotion, 0..20
          %span
            0表示为黑名单，发布的所有资源均为block,20表示为白名单，发布的资源均不需要审核。
      %tr
        %td
          
        %td
          - unless user.new_record?
            .right
              = link_to "删除", user_url(user), :title => "删除", :method => "delete", :confirm => "点击确定继续"
          = submit_tag("确定", :class => "submit")
          = link_to "返回", :back
            
          
            
          